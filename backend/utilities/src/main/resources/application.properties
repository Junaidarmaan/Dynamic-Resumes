spring.application.name=utilities
# App
server.port=8080

# MySQL datasource
spring.datasource.url=jdbc:mysql://localhost:3306/utilities ?useSSL=false&serverTimezone=UTC
spring.datasource.username=haisy
spring.datasource.password=junnu1522
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Disable SQL init scripts (important for now)
spring.sql.init.mode=never
