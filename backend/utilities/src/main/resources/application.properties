spring.application.name=utilities

# Server
server.port=8080

# MySQL datasource (FIXED)
spring.datasource.url=jdbc:mysql://localhost:3306/utilities?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=abubakar$
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# REMOVE dialect — Hibernate auto-detects it
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Disable SQL init scripts
spring.sql.init.mode=never